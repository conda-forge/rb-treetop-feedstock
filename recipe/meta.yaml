{% set name = "treetop" %}
{% set version = "1.6.17" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  - url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
    sha256: 7f52b5ffad949c974be091efb96d814a7c7a008e55e81d93bf267a73b62d2ca5

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem

requirements:
  host:
    - ruby
  run:
    - rb-polyglot >=0.3,<0.4
    - {{ pin_compatible('ruby', max_pin='x.x') }}

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'

about:
  home: https://rubygems.org/gems/treetop
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A Parsing Expression Grammar (PEG) Parser generator DSL for Ruby
  doc_url: http://www.rubydoc.info/gems/treetop
  dev_url: https://github.com/cjheath/treetop

extra:
  recipe-maintainers:
    - jdmarques
